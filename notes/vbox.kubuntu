
* Disable caps lock
* Install Gvim
* Install Terminator
* Install Netbeans / vim plugin

.bashrc
   cd ~
   mv .bashrc .bashrc.orig
   ln -s .arnold/jung_environment/bashrc .bashrc

vim
  mkdir ~/.vimbak
  ln -s .arnold/vim/vimrc .vimrc


Guest Additions
  #  sudo apt-get update
  #  sudo apt-get upgrade
  #  sudo apt-get install build-essential module-assistant
  #  sudo m-a prepare
  then install CD

